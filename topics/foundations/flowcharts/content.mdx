# Flowcharts

A **flowchart** is a visual diagram that represents an algorithm or process using standardized symbols. It helps you plan, visualize, and communicate logic before writing code.

## Why Use Flowcharts?

| Benefit | Description |
|---------|-------------|
| **Visual Thinking** | See the logic before coding |
| **Communication** | Share ideas with non-programmers |
| **Debugging** | Spot logical errors early |
| **Documentation** | Explain how code works |
| **Planning** | Design before implementation |

---

## Standard Flowchart Symbols

| Symbol | Name | Purpose | Example |
|--------|------|---------|---------|
| ⬭ | **Oval** | Start / End | "Start", "End" |
| ▱ | **Parallelogram** | Input / Output | "Read x", "Print result" |
| ▭ | **Rectangle** | Process | "sum = a + b" |
| ◇ | **Diamond** | Decision | "Is x > 0?" |
| → | **Arrow** | Flow direction | Connects symbols |
| ⬡ | **Hexagon** | Preparation | "Set count = 0" |

---

## Example 1: Check if Number is Even or Odd

### Flowchart Structure

![Check Even or Odd Flowchart](/images/docs/even_odd_flowchart.png)

### Pseudocode

```text
START
    INPUT n
    IF n % 2 == 0 THEN
        PRINT "Even"
    ELSE
        PRINT "Odd"
    ENDIF
END
```

```multi
[javascript]
const n = parseInt(prompt("Enter a number: "));
if (n % 2 === 0) {
    console.log("Even");
} else {
    console.log("Odd");
}

[python]
n = int(input("Enter a number: "))
if n % 2 == 0:
    print("Even")
else:
    print("Odd")

[java]
import java.util.Scanner;

public class EvenOdd {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();
        if (n % 2 == 0) {
            System.out.println("Even");
        } else {
            System.out.println("Odd");
        }
    }
}

[cpp]
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    if (n % 2 == 0) {
        cout << "Even" << endl;
    } else {
        cout << "Odd" << endl;
    }
    return 0;
}
```

---

## Example 2: Find Maximum of Three Numbers

### Flowchart Structure

![Maximum of Three Numbers Flowchart](/images/docs/max_of_three_flowchart.png)

```multi
[javascript]
const a = 5, b = 9, c = 3;
let maxVal;

if (a > b) {
    if (a > c) {
        maxVal = a;
    } else {
        maxVal = c;
    }
} else {
    if (b > c) {
        maxVal = b;
    } else {
        maxVal = c;
    }
}

console.log(`Maximum: ${maxVal}`);

[python]
a, b, c = 5, 9, 3

if a > b:
    if a > c:
        max_val = a
    else:
        max_val = c
else:
    if b > c:
        max_val = b
    else:
        max_val = c

print(f"Maximum: {max_val}")

[java]
int a = 5, b = 9, c = 3;
int maxVal;

if (a > b) {
    if (a > c) {
        maxVal = a;
    } else {
        maxVal = c;
    }
} else {
    if (b > c) {
        maxVal = b;
    } else {
        maxVal = c;
    }
}

System.out.println("Maximum: " + maxVal);

[cpp]
int a = 5, b = 9, c = 3;
int maxVal;

if (a > b) {
    if (a > c) {
        maxVal = a;
    } else {
        maxVal = c;
    }
} else {
    if (b > c) {
        maxVal = b;
    } else {
        maxVal = c;
    }
}

cout << "Maximum: " << maxVal << endl;
```

---

## Example 3: Loop - Sum of N Numbers

### Flowchart Structure

![Sum of N Numbers Flowchart](/images/docs/sum_n_numbers_flowchart.png)

### Pseudocode

```text
START
    INPUT n
    SET sum = 0
    SET i = 1
    
    WHILE i <= n DO
        sum = sum + i
        i = i + 1
    ENDWHILE
    
    PRINT sum
END
```

```multi
[javascript]
const n = parseInt(prompt("Enter n: "));
let total = 0;
for (let i = 1; i <= n; i++) {
    total += i;
}
console.log(`Sum: ${total}`);

[python]
n = int(input("Enter n: "))
total = 0
for i in range(1, n + 1):
    total += i
print(f"Sum: {total}")

[java]
import java.util.Scanner;

public class SumNumbers {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();
        int total = 0;
        for (int i = 1; i <= n; i++) {
            total += i;
        }
        System.out.println("Sum: " + total);
    }
}

[cpp]
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    int total = 0;
    for (int i = 1; i <= n; i++) {
        total += i;
    }
    cout << "Sum: " << total << endl;
    return 0;
}
```

---

### Decision Structures

![Common Decision Structures](/images/docs/decision_structures_flowchart.png)

---

### Loop Structures

![Common Loop Structures](/images/docs/loop_structures_flowchart.png)

---

## Best Practices

1. **Start Simple**: Begin with basic logic, add details later
2. **Top to Bottom**: Flow should generally go downward
3. **Left to Right**: Decisions branch left/right, then converge
4. **One Entry, One Exit**: Each symbol should have single entry/exit
5. **Use Connectors**: For complex flows, use numbered circles
6. **Keep It Clean**: Avoid crossing lines when possible

---

## Flowchart to Code Mapping

| Flowchart Element | Code Equivalent |
|-------------------|-----------------|
| Oval (Start) | `def function():` or `main()` |
| Parallelogram (Input) | `input()` or `read()` |
| Rectangle (Process) | Assignment, calculation |
| Diamond (Decision) | `if`, `elif`, `else` |
| Loop back arrow | `while`, `for` |
| Oval (End) | `return` or end of function |

---

## Key Takeaways

1. Flowcharts visualize **logic before coding**
2. Use **standard symbols** for clarity
3. **Diamonds** = Decisions (if/else)
4. **Loops** = Arrows that go back up
5. Always have **clear Start and End**
6. Great for **planning and debugging**
